services:
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:8000
    image: pactlify-frontend:0.4.0
    ports:
      - "5173:80"
